// @flow
import type { Color, CategorizedColorIdGrid, ColorList, ColorIdList, ColorMap, FamilyStructure } from './Colors'

export type ReduxAction = {
  type: string,
  payload: any
}

export type Section = {
  name: string,
  default: boolean,
  families: string[]
}

export type SectionCollection = Section[]

export type ColorsState = {
  status: {
    loading: boolean,
    error: boolean,
    activeRequest: boolean
  },
  items: {
    colors?: CategorizedColorIdGrid,
    brights?: CategorizedColorIdGrid,
    colorMap?: ColorMap,
    unorderedColors?: ColorIdList
  },
  structure?: FamilyStructure,
  sections?: string[],
  section?: string,
  families?: string[],
  family?: string,
  colorWallActive?: Color,
  search: {
    results?: ColorList,
    loading: boolean
  },
  initializeWith: {
    section?: string,
    family?: string,
    colorWallActive?: string
  }
}
