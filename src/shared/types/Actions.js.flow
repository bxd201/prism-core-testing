// @flow
import type { Color, CategorizedColorGrid, ColorList, ColorMap, FamilyStructure } from './Colors'

export type ReduxAction = {
  type: string,
  payload: any
}

export type Section = {
  name: string,
  default: boolean,
  families: string[]
}

export type SectionCollection = Section[]

export type ColorsState = {
  status: {
    loading: boolean,
    error: boolean,
    activeRequest: boolean
  },
  items: {
    colors?: CategorizedColorGrid,
    brights?: CategorizedColorGrid,
    colorMap?: ColorMap,
    unorderedColors?: ColorList
  },

  structure?: FamilyStructure,
  sections?: string[],
  section?: string,
  families?: string[],
  family?: string,
  colorWallActive?: Color,

  searchResults: ColorList,

  initializeWith: {
    section?: string,
    family?: string,
    colorWallActive?: string
  }
}
