// @flow
import type { Color, ColorSetPayload, ColorListPayload, ColorMap, FamilyStructure } from './Colors'

export type ReduxAction = {
  type: string,
  payload: any
}

export type Section = {
  name: string,
  families: string[]
}

export type SectionCollection = Section[]

export type ColorsState = {
  status: {
    loading: boolean,
    error: boolean,
    activeRequest: boolean
  },
  items: {
    colors?: ColorSetPayload,
    brights?: ColorSetPayload,
    colorMap?: ColorMap
  },
  family: {
    structure?: FamilyStructure,
    sections?: string,
    section?: string,
    seekingSection?: string,
    families?: string[],
    family?: string,
    seekingFamily?: string,
  },
  searchResults: ColorListPayload,
  colorWallActive?: Color,
  seekingColorWallActive?: string
}
